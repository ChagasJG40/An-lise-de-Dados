import anthropic

client = anthropic.Anthropic(api_key="SUA_API_KEY")

response = client.messages.create(
    model="claude-sonnet-5",
    max_tokens=1000,
    messages=[
        {"role": "user", "content": "Resuma este conjunto de dados: ..."}
    ]
)

print(response.content[0].text)
